using System;

namespace EasyTools.ObjectManagingTools
{
    public enum DuplicateMode { UniqueOnly, AllowDuplicates }
    public enum ReplaceExisting { Yes, No }

    public enum ManagerOrder { Ascending, Descending }
}